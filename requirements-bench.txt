# Benchmark-only dependencies (scripts/bench_cpu_serving.py).
#
# Deliberately NOT in requirements.txt: the application calls the embedding and
# rerank models over HTTP and never imports torch, so adding these would put
# ~2GB of unused wheels into the production image. Install only when measuring
# local model serving.
torch>=2.4
FlagEmbedding>=1.3

# scripts/bench_rerank_alternatives.py's onnx-export/onnx-bench/quality-onnx
# modes — measures the ONNX Runtime int8 quantization path (see
# .superpowers/sdd/rerank-alternatives-report.md). Ships a CoreMLExecutionProvider
# on this Mac's onnxruntime wheel, which is how --provider CoreMLExecutionProvider
# is reachable without a separate install.
onnx>=1.16
onnxruntime>=1.19
