# FastAPI 백엔드 주소. 백엔드의 cors_origins에 이 프론트의 오리진이
# 들어 있어야 세션 쿠키가 오간다 (app/config.py 참조).
# 컨테이너 스택을 볼 때는 프록시의 /api 를 쓴다: http://localhost:8088/api
# 호스트에서 uvicorn 을 직접 띄웠다면 그 주소를 그대로 쓴다.
NEXT_PUBLIC_API_BASE=http://localhost:8000
